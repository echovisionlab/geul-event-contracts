// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: api/open/v1/page.proto

package openv1connect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	v1 "github.com/echovisionlab/geul-event-contracts/gen/api/open/v1"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// PageServiceName is the fully-qualified name of the PageService service.
	PageServiceName = "api.open.v1.PageService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// PageServiceGetProcedure is the fully-qualified name of the PageService's Get RPC.
	PageServiceGetProcedure = "/api.open.v1.PageService/Get"
)

// PageServiceClient is a client for the api.open.v1.PageService service.
type PageServiceClient interface {
	// Get retrieves a page by slug
	// - slug="/" returns homepage page (or empty if not configured)
	// - slug="about" returns the page with that slug
	// - share_token allows access to draft pages
	Get(context.Context, *connect.Request[v1.GetPageRequest]) (*connect.Response[v1.GetPageResponse], error)
}

// NewPageServiceClient constructs a client for the api.open.v1.PageService service. By default, it
// uses the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewPageServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) PageServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	pageServiceMethods := v1.File_api_open_v1_page_proto.Services().ByName("PageService").Methods()
	return &pageServiceClient{
		get: connect.NewClient[v1.GetPageRequest, v1.GetPageResponse](
			httpClient,
			baseURL+PageServiceGetProcedure,
			connect.WithSchema(pageServiceMethods.ByName("Get")),
			connect.WithClientOptions(opts...),
		),
	}
}

// pageServiceClient implements PageServiceClient.
type pageServiceClient struct {
	get *connect.Client[v1.GetPageRequest, v1.GetPageResponse]
}

// Get calls api.open.v1.PageService.Get.
func (c *pageServiceClient) Get(ctx context.Context, req *connect.Request[v1.GetPageRequest]) (*connect.Response[v1.GetPageResponse], error) {
	return c.get.CallUnary(ctx, req)
}

// PageServiceHandler is an implementation of the api.open.v1.PageService service.
type PageServiceHandler interface {
	// Get retrieves a page by slug
	// - slug="/" returns homepage page (or empty if not configured)
	// - slug="about" returns the page with that slug
	// - share_token allows access to draft pages
	Get(context.Context, *connect.Request[v1.GetPageRequest]) (*connect.Response[v1.GetPageResponse], error)
}

// NewPageServiceHandler builds an HTTP handler from the service implementation. It returns the path
// on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewPageServiceHandler(svc PageServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	pageServiceMethods := v1.File_api_open_v1_page_proto.Services().ByName("PageService").Methods()
	pageServiceGetHandler := connect.NewUnaryHandler(
		PageServiceGetProcedure,
		svc.Get,
		connect.WithSchema(pageServiceMethods.ByName("Get")),
		connect.WithHandlerOptions(opts...),
	)
	return "/api.open.v1.PageService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case PageServiceGetProcedure:
			pageServiceGetHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedPageServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedPageServiceHandler struct{}

func (UnimplementedPageServiceHandler) Get(context.Context, *connect.Request[v1.GetPageRequest]) (*connect.Response[v1.GetPageResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("api.open.v1.PageService.Get is not implemented"))
}
