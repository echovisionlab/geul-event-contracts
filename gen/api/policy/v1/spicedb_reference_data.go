// Code generated by scripts/generated/generate-spicedb-catalog.mjs.
// DO NOT EDIT.
//
// Domain: reference_data — Shared reference-data resource policy

package policyv1

type mapPlaceDomain struct{}

// MapPlace is the closed map_place authorization namespace.
var MapPlace = mapPlaceDomain{}

// Resource constructs one typed map_place resource descriptor.
func (mapPlaceDomain) Resource(id string) (Resource, error) {
	return newResource("map_place", id)
}

// View describes the view business action on one map_place.
func (mapPlaceDomain) View(id string) (Can, error) {
	return newCan("map_place", id, "view", "view")
}

// Edit describes the edit business action on one map_place.
func (mapPlaceDomain) Edit(id string) (Can, error) {
	return newCan("map_place", id, "edit", "edit")
}

// Delete describes the delete business action on one map_place.
func (mapPlaceDomain) Delete(id string) (Can, error) {
	return newCan("map_place", id, "delete", "delete")
}

// Manage describes the manage business action on one map_place.
func (mapPlaceDomain) Manage(id string) (Can, error) {
	return newCan("map_place", id, "manage", "manage")
}

// Create describes the map_place.create business action on platform:global.
func (mapPlaceDomain) Create() (Can, error) {
	return newCan("platform", "global", "map_place.create", "is_author")
}

// Snapshot describes the exact typed relationship-read boundary for one map_place.
func (mapPlaceDomain) Snapshot(id string) (RelationshipSnapshotPlan, error) {
	return newRelationshipSnapshotPlan("map_place", id, "platform_admin", "")
}

// TouchPolicy creates a closed touch descriptor for map_place.policy.
func (mapPlaceDomain) TouchPolicy(id string) (RelationshipMutation, error) {
	resource, err := newResource("map_place", id)
	if err != nil {
		return RelationshipMutation{}, err
	}
	subject := platformRelationshipSubject()
	return newRelationshipMutation(resource, "policy", subject, RelationshipTouch)
}

// DeletePolicy creates a closed delete descriptor for map_place.policy.
func (mapPlaceDomain) DeletePolicy(id string) (RelationshipMutation, error) {
	resource, err := newResource("map_place", id)
	if err != nil {
		return RelationshipMutation{}, err
	}
	subject := platformRelationshipSubject()
	return newRelationshipMutation(resource, "policy", subject, RelationshipDelete)
}

type categoryDomain struct{}

// Category is the closed category authorization namespace.
var Category = categoryDomain{}

// Resource constructs one typed category resource descriptor.
func (categoryDomain) Resource(id string) (Resource, error) {
	return newResource("category", id)
}

// View describes the view business action on one category.
func (categoryDomain) View(id string) (Can, error) {
	return newCan("category", id, "view", "view")
}

// Edit describes the edit business action on one category.
func (categoryDomain) Edit(id string) (Can, error) {
	return newCan("category", id, "edit", "edit")
}

// Delete describes the delete business action on one category.
func (categoryDomain) Delete(id string) (Can, error) {
	return newCan("category", id, "delete", "delete")
}

// Manage describes the manage business action on one category.
func (categoryDomain) Manage(id string) (Can, error) {
	return newCan("category", id, "manage", "manage")
}

// Create describes the category.create business action on platform:global.
func (categoryDomain) Create() (Can, error) {
	return newCan("platform", "global", "category.create", "is_admin")
}

// List describes the category.list business action on platform:global.
func (categoryDomain) List() (Can, error) {
	return newCan("platform", "global", "category.list", "is_admin")
}

// Snapshot describes the exact typed relationship-read boundary for one category.
func (categoryDomain) Snapshot(id string) (RelationshipSnapshotPlan, error) {
	return newRelationshipSnapshotPlan("category", id, "platform_admin", "")
}

// TouchPolicy creates a closed touch descriptor for category.policy.
func (categoryDomain) TouchPolicy(id string) (RelationshipMutation, error) {
	resource, err := newResource("category", id)
	if err != nil {
		return RelationshipMutation{}, err
	}
	subject := platformRelationshipSubject()
	return newRelationshipMutation(resource, "policy", subject, RelationshipTouch)
}

// DeletePolicy creates a closed delete descriptor for category.policy.
func (categoryDomain) DeletePolicy(id string) (RelationshipMutation, error) {
	resource, err := newResource("category", id)
	if err != nil {
		return RelationshipMutation{}, err
	}
	subject := platformRelationshipSubject()
	return newRelationshipMutation(resource, "policy", subject, RelationshipDelete)
}

type tagDomain struct{}

// Tag is the closed tag authorization namespace.
var Tag = tagDomain{}

// Resource constructs one typed tag resource descriptor.
func (tagDomain) Resource(id string) (Resource, error) {
	return newResource("tag", id)
}

// View describes the view business action on one tag.
func (tagDomain) View(id string) (Can, error) {
	return newCan("tag", id, "view", "view")
}

// Edit describes the edit business action on one tag.
func (tagDomain) Edit(id string) (Can, error) {
	return newCan("tag", id, "edit", "edit")
}

// Delete describes the delete business action on one tag.
func (tagDomain) Delete(id string) (Can, error) {
	return newCan("tag", id, "delete", "delete")
}

// Manage describes the manage business action on one tag.
func (tagDomain) Manage(id string) (Can, error) {
	return newCan("tag", id, "manage", "manage")
}

// Create describes the tag.create business action on platform:global.
func (tagDomain) Create() (Can, error) {
	return newCan("platform", "global", "tag.create", "is_admin")
}

// List describes the tag.list business action on platform:global.
func (tagDomain) List() (Can, error) {
	return newCan("platform", "global", "tag.list", "is_admin")
}

// Snapshot describes the exact typed relationship-read boundary for one tag.
func (tagDomain) Snapshot(id string) (RelationshipSnapshotPlan, error) {
	return newRelationshipSnapshotPlan("tag", id, "platform_admin", "")
}

// TouchPolicy creates a closed touch descriptor for tag.policy.
func (tagDomain) TouchPolicy(id string) (RelationshipMutation, error) {
	resource, err := newResource("tag", id)
	if err != nil {
		return RelationshipMutation{}, err
	}
	subject := platformRelationshipSubject()
	return newRelationshipMutation(resource, "policy", subject, RelationshipTouch)
}

// DeletePolicy creates a closed delete descriptor for tag.policy.
func (tagDomain) DeletePolicy(id string) (RelationshipMutation, error) {
	resource, err := newResource("tag", id)
	if err != nil {
		return RelationshipMutation{}, err
	}
	subject := platformRelationshipSubject()
	return newRelationshipMutation(resource, "policy", subject, RelationshipDelete)
}

type genreDomain struct{}

// Genre is the closed genre authorization namespace.
var Genre = genreDomain{}

// Resource constructs one typed genre resource descriptor.
func (genreDomain) Resource(id string) (Resource, error) {
	return newResource("genre", id)
}

// View describes the view business action on one genre.
func (genreDomain) View(id string) (Can, error) {
	return newCan("genre", id, "view", "view")
}

// Edit describes the edit business action on one genre.
func (genreDomain) Edit(id string) (Can, error) {
	return newCan("genre", id, "edit", "edit")
}

// Delete describes the delete business action on one genre.
func (genreDomain) Delete(id string) (Can, error) {
	return newCan("genre", id, "delete", "delete")
}

// Manage describes the manage business action on one genre.
func (genreDomain) Manage(id string) (Can, error) {
	return newCan("genre", id, "manage", "manage")
}

// Create describes the genre.create business action on platform:global.
func (genreDomain) Create() (Can, error) {
	return newCan("platform", "global", "genre.create", "is_admin")
}

// List describes the genre.list business action on platform:global.
func (genreDomain) List() (Can, error) {
	return newCan("platform", "global", "genre.list", "is_admin")
}

// Snapshot describes the exact typed relationship-read boundary for one genre.
func (genreDomain) Snapshot(id string) (RelationshipSnapshotPlan, error) {
	return newRelationshipSnapshotPlan("genre", id, "platform_admin", "")
}

// TouchPolicy creates a closed touch descriptor for genre.policy.
func (genreDomain) TouchPolicy(id string) (RelationshipMutation, error) {
	resource, err := newResource("genre", id)
	if err != nil {
		return RelationshipMutation{}, err
	}
	subject := platformRelationshipSubject()
	return newRelationshipMutation(resource, "policy", subject, RelationshipTouch)
}

// DeletePolicy creates a closed delete descriptor for genre.policy.
func (genreDomain) DeletePolicy(id string) (RelationshipMutation, error) {
	resource, err := newResource("genre", id)
	if err != nil {
		return RelationshipMutation{}, err
	}
	subject := platformRelationshipSubject()
	return newRelationshipMutation(resource, "policy", subject, RelationshipDelete)
}

type styleDomain struct{}

// Style is the closed style authorization namespace.
var Style = styleDomain{}

// Resource constructs one typed style resource descriptor.
func (styleDomain) Resource(id string) (Resource, error) {
	return newResource("style", id)
}

// View describes the view business action on one style.
func (styleDomain) View(id string) (Can, error) {
	return newCan("style", id, "view", "view")
}

// Edit describes the edit business action on one style.
func (styleDomain) Edit(id string) (Can, error) {
	return newCan("style", id, "edit", "edit")
}

// Delete describes the delete business action on one style.
func (styleDomain) Delete(id string) (Can, error) {
	return newCan("style", id, "delete", "delete")
}

// Manage describes the manage business action on one style.
func (styleDomain) Manage(id string) (Can, error) {
	return newCan("style", id, "manage", "manage")
}

// Create describes the style.create business action on platform:global.
func (styleDomain) Create() (Can, error) {
	return newCan("platform", "global", "style.create", "is_admin")
}

// List describes the style.list business action on platform:global.
func (styleDomain) List() (Can, error) {
	return newCan("platform", "global", "style.list", "is_admin")
}

// Snapshot describes the exact typed relationship-read boundary for one style.
func (styleDomain) Snapshot(id string) (RelationshipSnapshotPlan, error) {
	return newRelationshipSnapshotPlan("style", id, "platform_admin", "")
}

// TouchPolicy creates a closed touch descriptor for style.policy.
func (styleDomain) TouchPolicy(id string) (RelationshipMutation, error) {
	resource, err := newResource("style", id)
	if err != nil {
		return RelationshipMutation{}, err
	}
	subject := platformRelationshipSubject()
	return newRelationshipMutation(resource, "policy", subject, RelationshipTouch)
}

// DeletePolicy creates a closed delete descriptor for style.policy.
func (styleDomain) DeletePolicy(id string) (RelationshipMutation, error) {
	resource, err := newResource("style", id)
	if err != nil {
		return RelationshipMutation{}, err
	}
	subject := platformRelationshipSubject()
	return newRelationshipMutation(resource, "policy", subject, RelationshipDelete)
}

type formatDomain struct{}

// Format is the closed format authorization namespace.
var Format = formatDomain{}

// Resource constructs one typed format resource descriptor.
func (formatDomain) Resource(id string) (Resource, error) {
	return newResource("format", id)
}

// View describes the view business action on one format.
func (formatDomain) View(id string) (Can, error) {
	return newCan("format", id, "view", "view")
}

// Edit describes the edit business action on one format.
func (formatDomain) Edit(id string) (Can, error) {
	return newCan("format", id, "edit", "edit")
}

// Delete describes the delete business action on one format.
func (formatDomain) Delete(id string) (Can, error) {
	return newCan("format", id, "delete", "delete")
}

// Manage describes the manage business action on one format.
func (formatDomain) Manage(id string) (Can, error) {
	return newCan("format", id, "manage", "manage")
}

// Create describes the format.create business action on platform:global.
func (formatDomain) Create() (Can, error) {
	return newCan("platform", "global", "format.create", "is_admin")
}

// List describes the format.list business action on platform:global.
func (formatDomain) List() (Can, error) {
	return newCan("platform", "global", "format.list", "is_admin")
}

// Snapshot describes the exact typed relationship-read boundary for one format.
func (formatDomain) Snapshot(id string) (RelationshipSnapshotPlan, error) {
	return newRelationshipSnapshotPlan("format", id, "platform_admin", "")
}

// TouchPolicy creates a closed touch descriptor for format.policy.
func (formatDomain) TouchPolicy(id string) (RelationshipMutation, error) {
	resource, err := newResource("format", id)
	if err != nil {
		return RelationshipMutation{}, err
	}
	subject := platformRelationshipSubject()
	return newRelationshipMutation(resource, "policy", subject, RelationshipTouch)
}

// DeletePolicy creates a closed delete descriptor for format.policy.
func (formatDomain) DeletePolicy(id string) (RelationshipMutation, error) {
	resource, err := newResource("format", id)
	if err != nil {
		return RelationshipMutation{}, err
	}
	subject := platformRelationshipSubject()
	return newRelationshipMutation(resource, "policy", subject, RelationshipDelete)
}

type clientDomain struct{}

// Client is the closed client authorization namespace.
var Client = clientDomain{}

// Resource constructs one typed client resource descriptor.
func (clientDomain) Resource(id string) (Resource, error) {
	return newResource("client", id)
}

// View describes the view business action on one client.
func (clientDomain) View(id string) (Can, error) {
	return newCan("client", id, "view", "view")
}

// Edit describes the edit business action on one client.
func (clientDomain) Edit(id string) (Can, error) {
	return newCan("client", id, "edit", "edit")
}

// Delete describes the delete business action on one client.
func (clientDomain) Delete(id string) (Can, error) {
	return newCan("client", id, "delete", "delete")
}

// Manage describes the manage business action on one client.
func (clientDomain) Manage(id string) (Can, error) {
	return newCan("client", id, "manage", "manage")
}

// Create describes the client.create business action on platform:global.
func (clientDomain) Create() (Can, error) {
	return newCan("platform", "global", "client.create", "is_admin")
}

// List describes the client.list business action on platform:global.
func (clientDomain) List() (Can, error) {
	return newCan("platform", "global", "client.list", "is_admin")
}

// Snapshot describes the exact typed relationship-read boundary for one client.
func (clientDomain) Snapshot(id string) (RelationshipSnapshotPlan, error) {
	return newRelationshipSnapshotPlan("client", id, "platform_admin", "")
}

// TouchPolicy creates a closed touch descriptor for client.policy.
func (clientDomain) TouchPolicy(id string) (RelationshipMutation, error) {
	resource, err := newResource("client", id)
	if err != nil {
		return RelationshipMutation{}, err
	}
	subject := platformRelationshipSubject()
	return newRelationshipMutation(resource, "policy", subject, RelationshipTouch)
}

// DeletePolicy creates a closed delete descriptor for client.policy.
func (clientDomain) DeletePolicy(id string) (RelationshipMutation, error) {
	resource, err := newResource("client", id)
	if err != nil {
		return RelationshipMutation{}, err
	}
	subject := platformRelationshipSubject()
	return newRelationshipMutation(resource, "policy", subject, RelationshipDelete)
}
